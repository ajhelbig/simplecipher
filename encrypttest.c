#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "encrypt.h"

int main(int argc, char *argv[])
{
	if(argc < 2) return 0;

	FILE *source = *fopen(argv[1], "r");

	FILE *source = *fopen(argv[2], "w");

}